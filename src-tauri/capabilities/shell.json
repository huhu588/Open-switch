{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shell-capability",
  "description": "允许应用使用 shell 插件打开外部链接",
  "windows": ["main"],
  "permissions": [
    "shell:allow-open"
  ]
}
